package com.kuchumof.fortailoring.constant

import com.kuchumof.fortailoring.R

/*
enum class TypeOfClothEnum(val id: Int, val translation: Int) {
    DRESS(1, R.string.Dress),
    SUIT(2, R.string.SUIT),
    HAT(3, R.string.HAT),
    SKIRT(4, R.string.SKIRT),
    T_SHIRT(5, R.string.T_SHIRT),
    BLOUSE(6, R.string.BLOUSE),
    PANTS(7, R.string.PANTS),
    HOODIE(8, R.string.HOODIE);

    companion object {

        fun fromId(id: Int): TypeOfClothEnum? {
            return values().firstOrNull { it.id == id }
        }
    }

    private var translation: Int

    constructor(translation: Int) {
        this.translation = translation
    }

    fun getTranslation(): Int {
        return translation
    }

}*/
